<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <title>IPHONE TEST</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
    <!-- <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, viewport-fit=cover" /> -->
    <!-- html {
        padding: env(safe-area-inset);
    } -->
    <style>
        html,
        body {
            height: 100%;
            margin: 0 auto;
            overflow-x: hidden;
            background-image: url("https://thcm.herokuapp.com/lwc/resources/Background.png");
        }

        .bigBox {
            width: 100vw;
            height: 100%;
            margin: 0 auto;
            overflow-x: hidden;
        }

        .center {
            top: 50%;
            width: 100%;
            position: absolute;
            text-align: center;
        }

        .bigFont {
            font-size: 5rem;
        }

        .smallFont {
            font-size: 2.5rem;
        }
    </style>
</head>

<body>
    <div class="bigBox">
        <div class="center">
            <div class="bigFont">
                Hello-EJS
            </div>
            <div class="smallFont">
                [v2.15.5]
            </div>
            <div class="smallFont">
                <%= dttm %>
            </div>
        </div>
    </div>
</body>

<script>
    /*
    document.addEventListener('orientationchange', () => {
        document.documentElement.style.height = `initial`;
        setTimeout(() => {
            document.documentElement.style.width = `100vw`;
            document.documentElement.style.height = `100vh`;
            setTimeout(() => {
                // this line prevents the content from hiding behind the address bar
                window.scrollTo(0, 1);
            }, 500);
        }, 500);
    });
    */
    // document.querySelector(".bigBox").style.backgroundColor = "<%=color %>";
    /*
    */
</script>

</html>
